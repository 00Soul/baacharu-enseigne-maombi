(function() {
    var injectParams = [];

    var RegistrationController = function() {
    };

    RegistrationController.$inject = injectParams;

    angular.module('winch').controller('RegistrationController', RegistrationController);
}());
