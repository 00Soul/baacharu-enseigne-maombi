(function() {
    var injectParams = [];

    var LoginController = function() {
    };

    LoginController.$inject = injectParams;

    angular.module('winch').controller('LoginController', LoginController);
}());
